EXPO_PUBLIC_DB_FILE_NAME=readings.db
EXPO_PUBLIC_CONTENT_BASE_URL=http://localhost:8000
# Set to "true" in local dev (.env.local) to bypass the 12-hour throttle and test updates live on every reload
EXPO_PUBLIC_DISABLE_UPDATE_THROTTLE=false
